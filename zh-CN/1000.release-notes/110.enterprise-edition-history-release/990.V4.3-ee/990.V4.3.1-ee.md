# OceanBase 数据库代理企业版 V4.3.1

## V4.3.1 BP11

### 版本信息

* 发布时间：2026 年 01 月 30 日

* 版本号：V4.3.1 BP11

* RPM 版本号：4.3.1.11-32026012914

### 缺陷修复

* 修复 ODP 升级 grpc 时 Core 的问题。
* 修复单副本 MySQL 租户场景下，使用 replace LS 命令恢复用户日志流后，无法通过 ODP 登录 MySQL 租户的问题。

## V4.3.1 BP10

### 版本信息

* 发布时间：2026 年 01 月 09 日

* 版本号：V4.3.1 BP10

* RPM 版本号：4.3.1.10-112026010714

### 缺陷修复

* 修复运行 table 回归时出现内存不足的问题。
* 修复开启 QoS 功能时，同时执行不同用户的请求导致 ODP 内存泄漏的问题。
* 修复修改 enable_ldg，在 sqlite3 中未生效的问题。
* 修复监控项 `rpc_current_session` 中没有租户和集群信息的问题。
* 修复 ODP 日志不受日志配置控制（`syslog_level`），导致潜在的日志丢失的问题。

## V4.3.1 BP9

### 版本信息

* 发布时间：2025 年 11 月 24 日

* 版本号：V4.3.1 BP9

* RPM 版本号：4.3.1.9-32025112117

### 缺陷修复

* 修复注释中存在 `from` 任意字符时，ODP 解析错误导致业务 SQL 耗时高的问题。
* 修复 ODP 和 OceanBase 数据库的兼容性问题。
* 修复使用 VPN 网卡时未配置 IP hostname，导致 ODP Core 的问题。

## V4.3.1 BP8

### 版本信息

* 发布时间：2025 年 10 月 24 日

* 版本号：V4.3.1 BP8

* RPM 版本号：4.3.1.8-22025102319

### 发版目的

* 支持带全局索引的同义词路由。
* 适配堆表路由。

### 缺陷修复

* 修复通过 `show trace` 命令查看全链路信息时，不显示 ODP 部分耗时的问题。
* 修复使用 `service_name` 登录的场景下，`odp_current_session` 监控项出现负数，导致 OCP 报警连接数超过限制的问题。
* 修复执行 ps 二合一协议时，报错 `Unknown prepared statement handle` 的问题。
* 修复多列分区表场景下，ODP 获取分区信息失败导致路由不准的问题。

## V4.3.1 BP7

### 版本信息

* 发布时间：2025 年 09 月 19 日

* 版本号：V4.3.1 BP7

* RPM 版本号：4.3.1.7-42025091914

### 发版目的

新增 `rpc_enable_sub_req_log` 配置项控制是否在 `obproxy_slow.log` 和 `obproxy_digest.log` 日志中打印满足打印条件的 OBKV 子请求日志。

### 缺陷修复

* 修复 Binlog 访问模式下 ODP 异常退出的问题。
* 修复弱读查询 metaDB 导致获取已删除数据的问题。
* 修复循环执行 scan_all 导致断连接的问题。
* 修复 DML 语句中列名包含 `#` 字符导致路由不准的问题。
* 修复执行以 hint 开头的 SQL 语句导致 ODP core 的问题。
* 修复 getaddrinfo 系统调用返回超过 64 个 IP 导致 ODP 启动 core 的问题。
* 修复事务中大于 4k 的请求在解析重试时断连接的问题。
* 修复使用旁路导入 SDK 出现导入卡住的问题。

## V4.3.1 BP6

### 版本信息

* 发布时间：2025 年 07 月 25 日

* 版本号：V4.3.1 BP6

* RPM 版本号：4.3.1.6-22025072510

### 发版目的

新增 `enable_crash_error_log` 配置项，用于设置是否对 CRASH 信号拦截。配置项的具体介绍可参见 [enable_crash_error_log](../../../400.configuration-management/200.global-configuration-items/533.enable_crash_error_log.md)。

### 缺陷修复

* 修改 RPC 端口被占用导致启动失败的问题。

* 修复多 OBServer 节点情况下，会话网络断连后执行 COM_STMT_RESET 语句报错的问题。

* 修复 DML 语句中表名为关键字，导致 ODP 解析不到表名触发异常退出的问题。

* 修复 service_name 请求大于 16M 导致 ODP 阻塞（hung），最终触发断连的问题。

## V4.3.1 BP5

### 版本信息

* 发布时间：2025 年 05 月 23 日

* 版本号：V4.3.1 BP5

* RPM 版本号：obproxy-4.3.1.5-62025052214

### 发版目的

begin 命令开启的 multi-query 事务，支持按照 begin 后的第一条 SQL 准确路由。

### 缺陷修复

* 修复 Binlog 实例 drop 后，下游请求仍旧建立长连接的问题。

* 修复 aws ppv2 下发的 VIP 级别配置建连时不生效的问题。

* 修复 ODP 生成 coredump 文件失败的问题。

* 修复二合一协议下，使用 ODP 连接 OceanBase 集群，执行长匿名块出现断链接的问题。

* 修复使用 ODP V4.3.1 访问 3.x 版本 OceanBase 集群时，出现分区计算失败报错 `-4016` 的问题。

## V4.3.1 BP4 hotfix3

### 版本信息

* 发布时间：2025 年 04 月 22 日

* 版本号：V4.3.1 BP4 hotfix3

* RPM 版本号：obproxy-4.3.1.4-282025042210

### 缺陷修复

* 修复误判协调者导致断连的问题。

* 修复协议导致的内存问题。

* 修复执行 `select proxy_version()` 命令后结果不准确的问题。

## V4.3.1 BP4 hotfix2

### 版本信息

* 发布时间：2025 年 03 月 21 日

* 版本号：V4.3.1 BP4 hotfix2

* RPM 版本号：obproxy-4.3.1.4-242025032019

### 发版目的

* 增加 bolt 执行优化。

* 修改 `enable_single_leader_node_routing` 默认值为 `False`。

### 缺陷修复

* 修复 sysbench 压测时，ODP 报错 `errno = 9747` 的问题。

* 修复由于 OBServer 节点断连接导致 Binlog 请求 core 的问题。

## V4.3.1 BP4 hotfix1

### 版本信息

* 发布时间：2025 年 03 月 05 日

* 版本号：V4.3.1 BP4 hotfix1

* RPM 版本号：obproxy-4.3.1.4-182025030514

### 缺陷修复

* 修复偶发的 ODP 不回包问题。

* 修复 Hbase 非分区表执行 put 操作报错的问题。

* 修复通过 IPV6 启动 ODP 失败，日志未打印的问题。

## V4.3.1 BP4

### 版本信息

* 发布时间：2025 年 01 月 24 日

* 版本号：V4.3.1 BP4

* RPM 版本号：obproxy-4.3.1.4-62025012317

### 缺陷修复

* 修复租户开通 Binlog 后，执行 `show master status` 命令偶发断连的问题。

* 修复 SQL 执行成功后，`obproxy_digest` 日志中记录 OceanBase 数据库耗时不准的问题。

* 修复 Prometheus 监测项 `odp_sql_cost_total` 存在统计值为负数的问题。

* 修复 ODP 与 OceanBase 数据库通信协议未对齐导致访问报错的问题。

* 修复 ODP 热重启时出现 core 的问题。

* 修复通过 ODP 连接集群时 hbase getscanners 返回结果数量不正确的问题。

* 修复 PL 块执行报错的问题。

* 修复 cs_id 分配失败导致 ODP core 的问题。

* 修复特定环境下执行 obproxy -V 命令报无依赖的问题。

* 修复 `show proxysession attribute xx` 的结果集中 `proxy_sessionid` 值出现负值的问题。

* 修复 ODP 日志中存在 `proxy_session_id` 值为负数的问题。

* 修复若干 ASAN 发现的内存写坏问题。

## V4.3.1 BP3

### 版本信息

* 发布时间：2024 年 11 月 15 日

* 版本号：V4.3.1 BP3

* RPM 版本号：obproxy-4.3.1.0-712024111215

### 发版目的

Service Name 支持新的登录识别方式，新增租户名关键字 Service。

### 缺陷修复

* 修复多个租户下的分区表具有相同的 tablet_id，导致的路由不准问题

* 修复生成列计算问题

* 修复 hbase reverse scan 下，使用 ODP 连接导致的单分区内有序，整体无序的问题。

## V4.3.1 BP2

### 版本信息

* 发布时间：2024 年 09 月 27 日

* 版本号：V4.3.1 BP2

* RPM 版本号：obproxy-4.3.1.0-662024092710

### 缺陷修复

* 修复 lsop 请求中的对于 batch put 处理结果的异常。

* 修复租户指定 `lower_case_table_names = 0`，并且数据库名称使用大小写组合场景下，业务访问断连接的问题。

* 修复租户指定 `lower_case_table_names = 0` 时，个别库表名无法准确路由的问题。

## V4.3.1 BP1 hotfix2

### 版本信息

* 发布时间：2024 年 09 月 05 日

* 版本号：V4.3.1 BP1 hotfix2

* RPM 版本号：obproxy-4.3.1.0-602024090512

### 缺陷修复

* 修复连接内多次执行 load data local infile 命令 hung 的问题。

* 修复使用 IPV6 部署 OceanBase 数据库时，ODP 连接失败的问题。

* 修复 GTID_SUBTRACT 函数不存在的问题。

* 修复 ObLocationTenantInfo 模块内存泄漏的问题。

* 修复 sequence 表查询中因缺乏对超时等异常逻辑的防御措施而导致的内存异常及程序崩溃问题。

* 修复高版本 MySQL 驱动 PS 协议参数全为 null，ODP 断连接的问题。

## V4.3.1 BP1 hotfix1

### 版本信息

* 发布时间：2024 年 08 月 13 日

* 版本号：V4.3.1 BP1 hotfix1

* RPM 版本号：obproxy-4.3.1.0-522024080914

### 缺陷修复

* 修复设置域名后 Binlog 访问失败的问题

* 修复使用 Sysbench 进行 DML 压测，会出现 core 导致重启的问题。

## V4.3.1 BP1

### 版本信息

* 发布时间：2024 年 08 月 01 日

* 版本号：V4.3.1 BP1

* RPM 版本号：obproxy-4.3.1.0-482024073020

### 发版目的

* 支持基于 substring 的 Key 分区路由

* Hbase 分区支持 substring_index 生成列作为分区键

### 缺陷修复

* 修复 Binlog 没有携带 connect attr 的问题。

* 修复 COM_STMT_SEND_LONG_DATA 连接断开后，ODP 需要断开客户端连接的问题。

## V4.3.1

### 版本信息

* 发布时间：2024 年 07 月 01 日

* 版本号：V4.3.1

* RPM 版本号：obproxy-4.3.1.0-402024062810

### 版本定位

ODP V4.3.1 是 ODP 4.3.x 系列的长期支持版本（LTS），推荐云上、线下业务用于 POC 测试、生产。SQL 代理新增主备库自动路由、指定多个 Zone 路由、ASCII 字符集路由、utf8mb4_0900_ai_ci 字符序路由等路由功能，新增 binlog_service_ip 支持域名及多地址功能，同时针对单机部署场景，优化路由策略，大幅提升性能。RPC 代理新增 HBase Empty Family 功能，完善 ObTableLSOps 请求类型，优化请求协议解析逻辑及路由信息批量获取的模式。

### 关键特性说明

#### SQL 代理

##### 支持主备库自动路由

在 OceanBase 数据库 2.x/3.x 版本的主备库方案中，CLUSTER_NAME 对应一组主备集群，用户登录时默认主集群登陆，如果发生主备切换，ODP 能自动路由到新的主集群，CLUSTER_NAME 不变，不需要业务修改登录连接串。在 OceanBase 数据库 4.x 版本中，为了充分利用 OceanBase 数据库多租户的特性，改为按照租户粒度提供物理备库能力，即主或备的角色信息是属于租户的，而不是集群级别，两个不同租户可能互为主备关系，同时租户的主备关系不在数据库中进行记录，需要通过外部组件（OCP）进行维护。

主备库切换后，需要业务修改连接串的租户名信息，才能正常登录，对业务有影响。所以在 ODP 4.3.1 版本开始，ODP 通过和 OCP 接口交互，内部记录租户级主备关系，关联不同的租户，再通过 service_name 连接串管理，屏蔽用户对租户级别的主备感知，实现跟 OceanBase 数据库 2.x/3.x 版本同样效果的主备库自动路由功能。

主备库自动路由的详细介绍可参见 [主备租户自动路由](../../../600.data-routing/450.primary-and-backup-tenant-routing.md)。

##### 指定多个 Zone 路由

ODP 的配置项 `proxy_primary_zone_name` 支持指定单个 Zone 路由，将 ODP 收到的所有请求都路由到指定的 Zone。OceanBase 数据库支持 OLAP、OLTP 混合负载的场景，需要 ODP 能够将分析型请求路由到 OLAP 的多个副本，做到负载均衡。因此，ODP V4.3.1 扩展了 `proxy_primary_zone_name` 指定多 Zone 路由能力。

##### 字符集/字符序扩展

OceanBase 数据库 4.2.4 版本支持了 ASCII 字符集、TIS620 字符集、utf8mb4_0900_ai_ci 字符序，相应的，ODP V4.3.1 也对这些字符集/字符序做了路由支持。

##### binlog_service_ip 支持域名及多地址

在 Binlog 服务和 ODP 的交互架构中，ODP 每次发送 Binlog 相关 SQL 请求或者 Binlog Dump 请求，都会先通过 binlog_service_ip 获取相应的 Binlog 服务集群地址，然后再发送请求到具体的 Binlog 服务集群。目前对于 binlog_service_ip 仅支持填写一个 IP 地址且不支持域名，功能简单，不能满足部分高可用场景的需求。ODP V4.3.1 支持了域名及多地址功能，实现了以下场景的高可用：

* 负载均衡（LB）地址在不同可用区的物理 IP 不一样

* LB 切换时，具备自动恢复能力

##### 性能优化

单机部署模式：ODP 是 OceanBase 数据库专用的服务代理，其核心目标是解决分布式集群多节点路由问题，路由策略在分布式场景下非常有优势，但在单机场景，路由需求简单，复杂的路由逻辑变得没有必要。因此，在 ODP 4.3.1 版本，针对单机场景，优化了路由逻辑，提升了路由性能。

编译优化：ODP V4.3.1 增加了一些系统编译相关的优化，进一步提升性能。

在单机模式下，ODP V4.3.1 相比 ODP V4.3.0，路由性能提升 30% - 50%。

#### RPC 代理

##### ObTableLSOps 请求功能完善

ODP V4.3.0 设计了一种新的单操作格式以及相应的批量格式，来承载 checkAndInsUp 的语义，支持 ObTableLSOps 请求。ODP V4.3.1 进一步完善，支持了 LSOP 带多种 TableOperation，依据 log stream 划分子请求功能，同时优化了性能。

##### HBase Empty Family

当前 OceanBase 数据库的 Hbase API 只支持单列簇，为了更好的支持多列簇，以及一些情况下不用修改客户代码兼容，比如客户端 scan 传入空列簇情况，在 HBASE 模式中，引入 table group 的概念，默认 HBASE 的 table 对应 OceanBase 数据库的 table group，HBASE 的 column family 对应 OceanBase 数据库的 table，使用这种方式来兼容 HBASE 列簇的概念。这种情况下，Client 发送到 ODP 的请求里，table name 的值对应了当前的 table group name，ODP 层需要支持这种 table group 路由，从而支持新的 HBASE 模式。

### 配置项变更

|              配置项               | 变更类型 |                                                                        变更说明                                                                        |
| --------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| enable_single_leader_node_routing | 新增     | 用于控制当租户为主副本聚集部署形态时，是否使用更高效的路由规则。                                                                                  |
| enable_full_link_trace            | 新增   | 用于控制是否使用全链路追踪功能来追踪查询执行。  |
| rpc_max_response_batch_size       | 新增   | 用于设置单次发送回包中最大的 batch 数量。  |
| rpc_max_request_batch_size        | 变更  | 该配置项自 4.3.1 版本起默认值由 `5`  更新为  `20`。  |
| enable_standby                    | 变更     | 该配置项自 4.3.1 版本起支持控制是否开启主备租户路由能力。                                                                                 |
| proxy_primary_zone_name           | 变更     | 该配置项自 4.3.1 版本起支持配置多个 Zone 名称。 |
| binlog_service_ip                 | 变更    | 该配置项自 4.3.1 版本起支持配置多个 IP 地址或域名。   |
| enable_strict_stat_time           | 变更    | 该配置项自 4.3.1 版本起默认值由 <code>True</code> 更新为 <code>False</code>。   |
| proxy_mem_limited                 | 变更     | 该配置项自 4.3.1 版本起取值范围由 <code>[100MB, 100G]</code> 更新为 <code>[100MB, +∞)</code>。 |

### 支持的 OceanBase 数据库版本

支持 OceanBase 数据库 4.x 和 3.x 以及更低版本。根据使用的 OceanBase 数据库版本不同，对应推荐如下：

* 当 OceanBase 数据库为 3.x 版本及更低版本时，建议使用 ODP V3.2.11。

* 当 OceanBase 数据库为 4.x 版本时，推荐使用 ODP V4.3.1（LTS 版本）。

### 缺陷修复

* 修复 ODP 连接集群，PS 二合一协议下，JDBC 执行 begin 语句夯住的问题。

* 修复主备库场景，第一次访问备集群失败后，再次访问持续报错失败的问题。
