# work_thread_num

`work_thread_num` 用于初始化工作线程，设置工作线程数，对 CPU 占用影响比较大，可根据环境动态调整，ODP 的 CPU 使用上限为 `work_thread_num` 的值。

|  属性    | 描述     |
|----------|---------|
| 参数类型 |  整数类型       |
| 默认值   | 128     |
| 取值范围 | [1, 128]  |
