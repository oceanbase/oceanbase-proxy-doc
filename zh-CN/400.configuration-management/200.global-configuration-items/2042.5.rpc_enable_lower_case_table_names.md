# rpc_enable_lower_case_table_names

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项自 4.3.4 版本开始引入。</p>
</main>

## 功能描述

`rpc_enable_lower_case_table_names` 用于控制 RPC 请求是否区分表名的大小写。

## 属性说明

|  属性    | 描述     |
|----------|---------|
| 参数类型 | 布尔类型      |
| 默认值   | True     |
| 取值范围 | <ul><li>True：区分表名大小写。</li><li>False：不区分表名大小写。</li></ul>  |
| 是否重启 ODP 生效 | 否  |

## 配置示例

执行如下命令，设置在 RPC 请求中不区分表名大小写。

```shell
obclient> alter proxyconfig set rpc_enable_lower_case_table_names='False';
```
