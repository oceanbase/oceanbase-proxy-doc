# rpc_request_server_entry_max_retries

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项自 4.3.0 版本开始引入。</p>
</main>

## 功能描述

`rpc_request_server_entry_max_retries` 用于设置 RPC 请求在 OceanBase 数据库表中失败后的重试次数。

## 属性说明

|  属性    | 描述     |
|----------|---------|
| 参数类型 | 整数类型      |
| 默认值   | 3     |
| 取值范围 | [1, +∞)  |
| 是否重启 ODP 生效 | 否  |

## 配置示例

执行如下命令，设置 RPC 请求在表中失败后重试次数为 5 次。

```shell
obclient> alter proxyconfig set rpc_request_server_entry_max_retries= 5;
```
