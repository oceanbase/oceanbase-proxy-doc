# tunnel_request_size_threshold

`tunnel_request_size_threshold` 用于设置 ODP 接收请求大小的阈值。当请求大小超过该阈值时，ODP 直接使用 tunnel 转发客户端请求，且不解析请求。取值为 0 时表示禁用。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置型从 V4.3.4 版本开始废弃。您可以查询并修改该配置项，但是该配置项并不会生效。</p>
</main>

|  属性    | 描述     |
|----------|---------|
| 参数类型 |  容量单位       |
| 默认值   | 8KB     |
| 取值范围 | (0, 16MB]  |
| 是否重启 ODP 生效 | 否  |

## 相关文档

[request_buffer_length](2010.request-buffer-length.md)
