| Description   |         |
|---------------|---------|
|machine-translation| en  |
|slug|rootservice_cluster_name|

# rootservice_cluster_name

根据登录方式的不同，`rootservice_cluster_name` 配置项有如下几种作用：

* 通过 Service Name 登录时，该配置项表示读写分离下的默认备集群。未配置的场景下，开启主备租户读写分离能力后，ODP 默认会将需要发给备租户的请求优先发给缓存中的第一个备租户。

* 非 Service Name 登录时，该配置项用于设置 `rootservice_list` 的默认集群名，或云用户的默认集群名。

<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>该配置项自 ODP 4.3.3 版本起更新，区分是否通过 Service Name 登录，通过 Service Name 登陆时，该配置项表示读写分离下的默认备集群。</p>
</main>

|  属性    | 描述     |
|----------|---------|
| 参数类型 |   字符串类型      |
| 默认值   | undefined     |
| 取值范围 | 不涉及  |
| 是否重启 ODP 生效 | 是  |

## 相关文档

* `rootservice_list` 的详细介绍可参见 [rootservice_list](../200.global-configuration-items/2030.rootservice_list.md)。

* 开启主备租户读写分离能力的配置项介绍可参见 [enable_standby_read_write_split](830.enable-standby-read-write-split.md)。

* Service Name 的相关介绍可参见 [主备租户自动路由](../../600.data-routing/450.primary-and-backup-tenant-routing.md)。
